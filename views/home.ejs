<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        *{
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            font-size:20px;

        }
        #btn{
            height:60px;
            width:150px;
            padding:10px;
            font-size:18px;
            border-radius:100px;
            box-shadow:5px 5px 8px black;
            
        }
        #int{
            height:40px;
            width:250px;
            box-shadow:5px 5px 8px black;
            border-radius:10px;
            margin-right:20px;
        }
        #btn:hover{
            background-color: antiquewhite;

        }
    </style>
</head>
<body>
    <h1>Home Page</h1>
    <h2>URL Shortner</h2>
    <div id="main">
        <form action="/url" method="post">
            <label>Enter your URL</label>
            <input type="url" name="url" id="int" placeholder="http://example.com">
            <input type="submit" value="submit" id="btn">
        </form>
    </div>
    <% if(locals.id){%>
        <p>URL Genrated=http://localhost:4002/url/<%= locals.id %></p>
    <%  }%>
    <% if(locals.users){ %>
        <table>
            <tr>
                <th>redirectUrl</th>
                <th> shortId</th>
                <th>Number-of-clicks</th>
            </tr>
            <% locals.users.forEach(user=>{ %>
                <tr>
                    <td><%=user.redirectUrl%></td>
                    <td><%= user.shortId %></td>
                    <td><%= user. vistHistory.length%></td>
                </tr>
            <% }) %>
        </table>
    <% } %>

</body>
</html>